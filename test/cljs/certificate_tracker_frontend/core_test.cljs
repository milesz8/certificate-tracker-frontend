(ns certificate-tracker-frontend.core-test
  (:require [cljs.test :refer-macros [deftest testing is]]
            [certificate-tracker-frontend.core :as core]))

(deftest fake-test
  (testing "fake description"
    (is (= 1 2))))
